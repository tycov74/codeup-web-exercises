console.log('Inside of Arrays');

// let ourFirstArray = [];
//
// let ourSecondArray = ['one', 'two', 'three'];
//
// let ourThirdArray = ['hello', true, 499, 3.14159, [1, 2, 3]];


// Continuing Arrays

//  let names = [ array of names]; use the ".length" to find of the length of the array
//     console.log(names.length) *no other () needs to be involved. If so it will break the code

// Accessing Array Elements
// console.log(names[0]);
// console.log(names[names.length - 1]);

// Iterating Arrays

// old school way with "index"
// for (let i = 0; i < names.length; i++) {
//     console.log(names[i]);
// }

// For...of Loop (Spicy Loop) New way of listing array of names
/* for(let name of names)
    console.log(name);
 */

// for Each Loop

/* names.forEach(name => ) {
    console.log(name);

});

 */

//  Spread Syntax
/*  const someArray = [...names] ===> spread syntax; can be used during function arguments / pass in arguments
    console.log(someArray);

    names.push('Another Name');
    console.log(names);

    console.log(someArray);

    function showCoordinates(lat, long) {
    console.log(`Latitude: ${lat}`);
    console.log(`Longitude: ${long}`);

    const coordinates = [         ,       ]
    showCoords(...coordinates); ==> spread of coordinates

    function

 */

/**
 *
 *
 */
