console.log('Inside of Arrays');

// let ourFirstArray = [];
//
// let ourSecondArray = ['one', 'two', 'three'];
//
// let ourThirdArray = ['hello', true, 499, 3.14159, [1, 2, 3]];


// Continuing Arrays

//  let names = [ array of names]; use the ".length" to find of the length of the array
//     console.log(names.length) *no other () needs to be involved. If so it will break the code

// Accessing Array Elements
// console.log(names[0]);
// console.log(names[names.length - 1]);

// Iterating Arrays

// for (let i = 0; i < names.length; i++) {
//     console.log(names[i]);
// }

// For...of Loop (Spicy Loop) New way of listing array of names
/* for(let name of names)
    console.log(name);
 */